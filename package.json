{
  "name": "devcrazino-workspace",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "test": "jest --runInBand",
    "lint": "echo \"No linter configured\""
  },
  "devDependencies": {
    "jest": "^29.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}
